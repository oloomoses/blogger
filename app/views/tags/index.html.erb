<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
  <li>
    <%= tag.name %>
    <%= link_to 'Delete', tag_path(tag), method: :delete, data: { confirm: "Confirm delete" } %>
  </li>
<% end %>
</ul>

<%= link_to "All articles", articles_path %>

